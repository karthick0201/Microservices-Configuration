server:
  port: 1122

spring:
  application:
    name: ADDRESS-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mageshit2025
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

message: Addressservice - Dev Config Loaded from Config Server